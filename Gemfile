source "https://rubygems.org"

# Pin the dependency set used by GitHub Pages for local parity.
gem "github-pages", group: :jekyll_plugins

# Required by `jekyll serve` on Ruby 3.
gem "webrick", "~> 1.8"


